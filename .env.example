# Note: avoid port 5000 on macOS — AirPlay Receiver holds it (causes "Failed to fetch")
PORT=5050
VITE_API_URL=http://localhost:5050

# Get a free key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
